import FullScene from '../components/FullScene'

const Stop = () => {
  const message = `Des bisous, prenez soin de vous !`

  return <FullScene message={message} sceneName="campfire" name="stop" />
}

export default Stop
